#ifndef EXPECTED_CONVERSION_H
#define EXPECTED_CONVERSION_H

    #include "sample_lookup.h"

    float deltaForRange(int r);

    void expectedConversion(SampleLookup **sl, int instrument, int pair, int range);

#endif